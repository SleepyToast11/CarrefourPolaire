@using Microsoft.AspNetCore.Localization
@using Microsoft.AspNetCore.Http.Features
@using Microsoft.AspNetCore.Authentication
@inject IHttpContextAccessor HttpContextAccessor
@{
    var user = HttpContextAccessor.HttpContext?.User;
    var isAuthenticated = user?.Identity?.IsAuthenticated ?? false;

    // Get current culture
    var cultureFeature = HttpContextAccessor.HttpContext?.Features.Get<IRequestCultureFeature>();
    var currentCulture = cultureFeature?.RequestCulture.Culture.TwoLetterISOLanguageName ?? "en";
    var oppositeCulture = currentCulture == "fr" ? "en" : "fr";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="icon" type="image/svg+xml" href="~/images/logo2.svg" />
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <title>@ViewData["Title"] - Carrefour Polaire</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <link rel="stylesheet" href="~/css/site.css"/>
    <style>
        /* This ensures the content + navbar take at least full viewport height,
           so the footer starts below the screen */
        .page-wrapper {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        main {
            flex-grow: 1;
        }
        </style>
</head>

<body class="bg-light text-dark">
<div class="page-wrapper min-height:100vh">

    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
        <div class="container">
            <!-- Brand with logo + optional name -->
            <a class="navbar-brand d-flex align-items-center" href="/">
                <img src="images/logo2.svg" alt="Carrefour Polaire" width="40" height="40" class="me-2">
                <span class="text-blue fw-bold">Carrefour Polaire</span>
            </a>

            <!-- Hamburger menu for mobile -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarPublic" aria-controls="navbarPublic" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <!-- Menu items -->
            <div class="collapse navbar-collapse" id="navbarPublic">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <!-- Menu link -->
                    <li class="nav-item">
                        <a class="nav-link" href="/Menu">Menu</a>
                    </li>
                    <!-- Optional: About link -->
                    <li class="nav-item">
                        <a class="nav-link" href="/About">About</a>
                    </li>
                    <!-- Optional: Contact link -->
                    <li class="nav-item">
                        <a class="nav-link" href="/Contact">Contact</a>
                    </li>
                </ul>

                <!-- Right-side info/sign in -->
                <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                    <li class="nav-item me-3">
                        <span class="navbar-text text-darkorange">Winter 2025 Event</span>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-orange" href="/Login">Sign In</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main role="main" class="flex-grow-1">
        <div class="container pt-4">
            @RenderBody()
        </div>
    </main>
</div>

<footer class="bg-dark text-light text-center py-3 mt-auto">
    <div class="container">
        <p>&copy; 2025 Carrefour Polaire. All rights reserved.</p>
        <p>Contact: <a href="mailto:info@carrefourpolaire.com" class="text-blue">info@carrefourpolaire.com</a></p>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
@RenderSection("Scripts", required: false)
</body>